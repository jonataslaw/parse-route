name: parse_route
description: All-encompassing solution for route parsing, matching, and navigation within Dart applications
version: 2.1.0
homepage: https://github.com/jonataslaw/parse-route

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:

dev_dependencies:
  test: ^1.25.8
  lints: ^4.0.0
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
